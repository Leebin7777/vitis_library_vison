#include <fstream>
#include <string>
#include <vector>		
#include <stdio.h>

int  add_(int a,int b){

    int c;
    c = a + b;
    return c;
}

void max(int * a, int* b){
    int a1 = a[0];
    int b1 = b[0];
    a[0] = a1 + b1;

}
